const {
    convertToTitleCase
} = require("./toTitleCase")

function convertToPascalCase(str) {
    return str.split(" ").map(name => convertToTitleCase(name)).join("")
}

module.exports = {
    convertToPascalCase,
}