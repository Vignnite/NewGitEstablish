// @ts-nocheck
import React, { FC } from "react";
import { Form, Formik } from "formik";
import { Row, Col, Button } from "antd";
import "./stylesFile.scss";
//model-import
//components-imports

interface ClassNameProps {
  data?: ModelName;
}

const ClassName: FC<ClassNameProps> = (props) => {
  const { data } = props;

  const onSubmit = (values: ModelName) => {
  };

  //return
};

export default ClassName;
